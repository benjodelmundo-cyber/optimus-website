import type { Metadata } from "next";import { Shell,PageHero,LegalCallout } from "../components";import { projects } from "../data";
export const metadata:Metadata={title:"Our Work",description:"Selected examples of Optimus competition, customization and premium gunsmithing craftsmanship."};
export default function Work(){return <Shell><PageHero eyebrow="Selected projects" title="Craftsmanship you can see." intro="A curated look at competition, finish and precision projects. Final project classifications and publication permissions remain subject to owner review."/><div className="content"><div className="gallery">{projects.map(p=><article key={p.title}><img src={p.image} alt={p.alt}/><div><h2>{p.title}</h2><p>Selected example of Optimus craftsmanship. Project details are intentionally limited to protect customer privacy.</p></div></article>)}</div><LegalCallout/></div></Shell>}
