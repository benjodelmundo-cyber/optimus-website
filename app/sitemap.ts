import type {MetadataRoute} from "next";import {services,site} from "./data";export default function sitemap():MetadataRoute.Sitemap{const routes=["","/services","/our-work","/about","/process","/faq","/request-a-consultation","/contact","/privacy","/terms","/disclaimer"];return [...routes.map(url=>({url:`${site.url}${url}`,lastModified:new Date()})),...services.map(s=>({url:`${site.url}/services/${s.slug}`,lastModified:new Date()}))]}
