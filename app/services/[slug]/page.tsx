import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { Shell, LegalCallout } from "../../components";
import { services, site } from "../../data";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);return {title:s?.name??"Service",description:s?.summary}}
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <Shell><div className="page-hero"><div className="breadcrumb"><Link href="/services">Services</Link> / {s.category}</div><p className="eyebrow">{s.category}</p><h1>{s.name}</h1><p className="lede">{s.summary}</p></div><div className="content detail-grid"><div><p className="eyebrow">What to expect</p><h2>An inspection-led, clearly scoped service.</h2><p className="large-copy">We begin by understanding the intended lawful sporting, reliability, preservation or presentation goal. Staff then assess condition, compatibility, available parts and the requirements that apply before confirming whether the work can be accepted.</p><h2>Scope and exclusions</h2><p>Specific inclusions are confirmed only after inspection. Optimus does not accept work intended to bypass safety features, alter identifying marks, evade legal controls or create prohibited configurations.</p><LegalCallout/></div><aside className="side-panel"><p className="eyebrow">Start a conversation</p><h2>Request a review</h2><p>Share the general goal without including serial numbers, permit details or other sensitive information.</p><Link className="button" href={`/request-a-consultation?service=${s.slug}`}>Request consultation</Link><hr/><small>{site.legalNotice}</small></aside></div></Shell>}
